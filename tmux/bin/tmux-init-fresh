#!/usr/bin/env zsh
# Fresh init session - kills existing and clears resurrect state

SESSION="init"

# Kill existing session (if any)
tmux kill-session -t "$SESSION" 2>/dev/null

# Clear tmux-resurrect state
rm -f ~/.local/share/tmux/resurrect/last

# Launch new session
exec tmux-init
